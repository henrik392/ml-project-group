# Custom ByteTrack configuration for exp07
# Based on exp07_bytetrack.yaml parameters

tracker_type: bytetrack
track_high_thresh: 0.5  # Increased from default 0.25
track_low_thresh: 0.1
new_track_thresh: 0.6   # Increased from default 0.25
track_buffer: 30
match_thresh: 0.8
fuse_score: True
